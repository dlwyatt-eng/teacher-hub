import practice from "../content/home-practice.json";
export default function HomePracticeTeacher() {
  return <section className="homework-policy"><header><div><p className="section-kicker">PUBLISHED HOME PRACTICE · {practice.updatedOn}</p><h2>{practice.week.label}</h2><p>{practice.week.connection}</p></div></header><p>{practice.policy}</p><p><a href="https://dlwyatt-eng.github.io/learn/#/homework" target="_blank" rel="noreferrer">Open the family menu and print activities ↗</a></p><details><summary>Review activities and understanding checks</summary><div className="homework-columns">{practice.week.activities.map(item => <article key={item.id}><small>{item.subject} · {item.time}</small><h3>{item.title}</h3><p>{item.context}</p><p><strong>Model:</strong> {item.example}</p><ol>{item.steps.map(step => <li key={step}>{step}</li>)}</ol><p><strong>Check:</strong> {item.check}</p></article>)}</div></details><footer><strong>Preparing the next menu</strong><p>Select skills actually taught. Retain the outgoing dated menu and publish a new date range with a model, short task, screen-free route and checking guidance. Keep individual adaptations private. Device-local day-plan edits do not publish family updates.</p></footer></section>;
}
